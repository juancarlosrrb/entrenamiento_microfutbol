# Crear una tabla virtual
tabla_virtual <- tbl(con, "nombre_de_tu_tabla")

# Definir el tamaño del chunk (por ejemplo, 100,000 filas)
tamano_chunk <- 100000

# Recuperar los datos en chunks
resultado <- tabla_virtual %>%
  mutate(chunk_id = row_number() %/% tamano_chunk) %>%
  group_by(chunk_id) %>%
  slice(seq_len(n())) %>%
  collect()
